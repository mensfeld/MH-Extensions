require 'psych'
require 'rubygems'
require 'rake'
require 'echoe'

Echoe.new('mh_extensions', '0.1.6.3') do |p|
  p.description    = "Package of usefull ruby basic classes (and not only) extensions"
  p.url            = "https://github.com/mensfeld/MH-Extensions"
  p.author         = "Maciej Mensfeld"
  p.email          = "maciej@mensfeld.pl"
  p.dependencies   = ["babosa"]
  p.ignore_pattern = ["tmp/*", "script/*"]
end

